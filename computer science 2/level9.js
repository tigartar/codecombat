while(true) {
    var enemy = hero.findNearestEnemy();
    if (enemy) {
hero.say("enemy spotted");  
    }
        if (enemy) {
         hero.attack(enemy);
        hero.attack(enemy);  
        }
}