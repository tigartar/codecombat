var gems = this.findItems();
while (this.pos.x < 20) {
	this.move({'x': 20, 'y': 35});
}

while (this.pos.x < 25) {
var	gem0 = gems[0];
	this.move(gem0.pos);
}
while (this.pos.x < 30) {
	this.move({'x': 30, 'y': 35});
}
while (this.pos.x < 35) {
var	gem1 = gems[1];
	this.move(gem1.pos);
}
while (this.pos.x < 40) {
	this.move({'x': 40, 'y': 35});
}
while (this.pos.x < 45) {
var	gem2 = gems[2];
	this.move(gem2.pos);
}
while (this.pos.x < 50) {
	this.move({'x': 50, 'y': 35});
}
while (this.pos.x < 55) {
var	gem3 = gems[3];
	this.move(gem3.pos);
}
